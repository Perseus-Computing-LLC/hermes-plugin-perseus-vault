name: perseus-vault
version: 1.1.0
description: "Perseus Vault — shared MCP memory plus optional Authorized Action Receipt enforcement for Hermes tools."
pip_dependencies:
  - mcp
requires_env:
  - PERSEUS_VAULT_MCP_TOKEN
